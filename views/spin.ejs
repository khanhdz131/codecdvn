<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Vòng quay Robux</title>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #0f2027, #203a43, #2c5364);
      color: #fff;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: rgba(0,0,0,0.3);
      padding: 10px 20px;
    }

    .back-btn {
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      background: #ff4c4c;
      padding: 8px 15px;
      border-radius: 8px;
      transition: 0.2s;
    }

    .back-btn:hover {
      background: #ff1f1f;
    }

    .balance {
      font-size: 16px;
      background: rgba(255, 255, 255, 0.1);
      padding: 8px 15px;
      border-radius: 10px;
    }

    .container {
      max-width: 1000px;
      margin: 30px auto;
      text-align: center;
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
      text-shadow: 2px 2px 4px #000;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
      padding: 20px;
    }

    .box {
      background: #1e1e1e;
      border-radius: 15px;
      padding: 10px;
      transition: transform 0.2s;
      cursor: pointer;
      box-shadow: 0 0 10px rgba(255,255,255,0.1);
    }

    .box:hover {
      transform: scale(1.08);
      box-shadow: 0 0 15px rgba(0,255,255,0.4);
    }

    .box img {
      width: 100%;
      border-radius: 10px;
    }

    .footer {
      margin-top: 40px;
      color: #ccc;
      font-size: 14px;
    }
  </style>
</head>
<body>

  <div class="header">
    <a href="/dashboard" class="back-btn">← Trang chủ</a>
    <div class="balance">
      💰 <strong>số dư:</strong> <%= user.balance %> | 💎 <strong>Robux:</strong> <%= user.robux %>
    </div>
  </div>

  <div class="container">
    <h1>VÒNG QUAY NHẬN ROBUX</h1>

    <div class="grid">
      <% for (let i = 0; i < 15; i++) { %>
        <div class="box" onclick="spinBox(<%= i %>)">
          <img src="/images/gift.jpg" alt="Hộp quà">
        </div>
      <% } %>
    </div>

    <div class="footer">Mỗi lượt quay tốn <strong>20.000 Xu</strong>. Chúc bạn may mắn!</div>
  </div>

  <script>
    async function spinBox(index) {
      const confirm = await Swal.fire({
        title: 'Bạn chắc chắn quay hộp này?',
        text: 'Sẽ trừ 20.000 Xu từ tài khoản của bạn!',
        icon: 'question',
        showCancelButton: true,
        confirmButtonText: 'Quay ngay!',
        cancelButtonText: 'Hủy'
      });

      if (!confirm.isConfirmed) return;

      const res = await fetch('/spin', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ index })
      });

      const data = await res.json();

      if (data.error) {
        return Swal.fire('Lỗi!', data.error, 'error');
      }

      await Swal.fire({
        title: '🎉 Bạn nhận được!',
        text: `${data.reward} Robux`,
        icon: 'success',
        confirmButtonText: 'OK'
      });

      location.reload();
    }
  </script>
</body>
</html>
