<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Lịch sử mua tài khoản</title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      background-color: #121212;
      color: #fff;
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 1000px;
      margin: 50px auto;
      padding: 30px;
      background: #1e1e1e;
      border-radius: 10px;
      box-shadow: 0 0 20px rgba(0,255,255,0.1);
    }
    h1 {
      text-align: center;
      color: #00ffff;
      margin-bottom: 30px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 15px;
    }
    th, td {
      border: 1px solid #333;
      padding: 12px;
      text-align: center;
    }
    th {
      background-color: #00bfff;
      color: white;
    }
    tr:nth-child(even) {
      background-color: #2c2c2c;
    }
    tr:hover {
      background-color: #3a3a3a;
    }
    .back {
      display: block;
      margin-top: 20px;
      text-align: center;
    }
    .back a {
      color: #0ff;
      text-decoration: none;
    }
  </style>
</head>
<body>

  <div class="container">
    <h1>📜 LỊCH SỬ MUA TÀI KHOẢN</h1>

    <% if (history.length === 0) { %>
      <p style="text-align: center;">Bạn chưa mua tài khoản nào.</p>
    <% } else { %>
      <table>
        <thead>
          <tr>
            <th>Tài khoản</th>
            <th>Mật khẩu</th>
            <th>Giá</th>
            <th>Thời gian</th>
          </tr>
        </thead>
        <tbody>
          <% history.forEach(item => { %>
            <tr>
              <td><%= item.taikhoan %></td>
              <td><%= item.matkhau %></td>
              <td><%= item.gia %> xu</td>
              <td><%= item.thoigian %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <div class="back">
      <a href="/dashboard">⬅ Quay lại trang chủ</a>
    </div>
  </div>

</body>
</html>
