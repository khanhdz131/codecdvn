<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - Shop Acc Roblox</title>
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    body {
      margin: 0;
      font-family: 'Montserrat', sans-serif;
      background-image: url("/images/bg1.jpg");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      min-height: 100vh;
      color: white;
      overflow-x: hidden;
      animation: bling 1s ease-in;
    }

    @keyframes bling {
      0% { opacity: 0; transform: scale(0.95); }
      100% { opacity: 1; transform: scale(1); }
    }

    .fireworks {
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      pointer-events: none;
      background: url('/images/gif2.jpg') center center no-repeat;
      background-size: cover;
      z-index: 999;
      animation: fadeout 2.5s forwards;
    }

    @keyframes fadeout {
      0% { opacity: 1; }
      100% { opacity: 0; display: none; }
    }

    .topbar {
      background: rgba(0,0,0,0.5);
      padding: 10px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .user-info {
      font-size: 14px;
    }

    .container {
      text-align: center;
      padding: 50px 20px;
      flex: 1;
    }

    .menu-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 30px;
      max-width: 1000px;
      margin: 0 auto;
    }

    .menu-box {
      background: rgba(0, 0, 0, 0.75);
      border-radius: 16px;
      padding: 25px 15px;
      text-align: center;
      color: white;
      text-decoration: none;
      transition: 0.3s;
      box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
      cursor: pointer;
      animation: fadeInUp 0.8s ease both;
    }

    .menu-box:hover {
      transform: scale(1.05);
      background: rgba(0, 0, 0, 0.85);
    }

    .menu-box div {
      font-size: 28px;
    }

    .menu-box span {
      margin-top: 10px;
      font-size: 16px;
      display: block;
    }

    .footer {
      text-align: center;
      background: rgba(0, 0, 0, 0.5);
      padding: 15px;
      font-size: 14px;
      color: #ccc;
    }

    .floating-support {
      position: fixed;
      bottom: 20px;
      right: 20px;
      background: #00cc88;
      padding: 10px 16px;
      border-radius: 999px;
      font-weight: bold;
      cursor: pointer;
      color: white;
      box-shadow: 0 0 10px rgba(0,0,0,0.3);
      transition: 0.3s;
      z-index: 1000;
    }

    .floating-support:hover {
      background: #00b377;
      transform: scale(1.05);
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>
  <div class="fireworks"></div>

  <div class="topbar">
    <div><strong>Shop Acc Roblox</strong></div>
    <div class="user-info">
      👤 <%= user.username %> | 💰 Xu: <%= user.balance %> | 💎 Robux: <%= user.robux %>
       <a href="/doimatkhau" style="color: #f99;">Đổi Mật Khẩu</a>
      | <a href="/logout" style="color: #f99;">Đăng xuất</a>
    </div>
  </div>

  <div class="container">
    <div class="menu-grid">
      <a href="/withdraw" class="menu-box">
        <div>🔁</div>
        <span>Rút Robux</span>
      </a>
      <a href="/spin" class="menu-box">
        <div>🎁</div>
        <span>Quay Hộp Quà</span>
      </a>
      <a href="/topup" class="menu-box">
        <div>💳</div>
        <span>Nạp Tiền</span>
      </a>
      <a href="/shop" class="menu-box">
        <div>🛒</div>
        <span>Shop Acc</span>
      </a>
      <a href="/history" class="menu-box">
        <div>📜</div>
        <span>Lịch Sử</span>
      </a>
      <div onclick="showSupport()" class="menu-box">
        <div>📩</div>
        <span>Hỗ Trợ</span>
      </div>
    </div>
  </div>

  <div class="footer">
    © 2025 Shop Acc Roblox - Uy tín, chất lượng, hỗ trợ 24/7
  </div>

  <div class="floating-support" onclick="showSupport()">💬 Hỗ Trợ</div>

  <script>
    function showSupport() {
      Swal.fire({
        title: 'Liên hệ hỗ trợ',
        html: `
          <p>📞 tiktok: <strong>robux_napre</strong></p>
          <p>📧 Email: <strong>robuxnapre@gmail.com</strong></p>
        
        `,
        icon: 'info',
        confirmButtonText: 'Đóng'
      });
    }
  </script>
</body>
</html>
