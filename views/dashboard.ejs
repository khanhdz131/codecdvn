<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body, html {
      margin: 0;
      padding: 0;
      height: 100%;
    }

    body {
      background: url('/images/bg1.jpg') no-repeat center center fixed;
      background-size: cover;
    }

    .overlay {
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(4px);
      width: 100%;
      height: 100%;
      position: absolute;
      top: 0; left: 0;
      z-index: 0;
    }

    .container {
      position: relative;
      max-width: 1100px;
      margin: auto;
      padding: 60px 20px;
      z-index: 1;
      color: #fff;
    }

    h1 {
      text-align: center;
      margin-bottom: 10px;
      font-size: 28px;
    }

    .balance-box {
      text-align: center;
      margin-bottom: 35px;
    }

    .balance-box p {
      font-size: 17px;
      margin: 5px 0;
      background: rgba(255, 255, 255, 0.15);
      display: inline-block;
      padding: 8px 18px;
      border-radius: 12px;
      backdrop-filter: blur(5px);
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 25px;
    }

    .card {
      background: rgba(255, 255, 255, 0.95);
      color: #333;
      border-radius: 16px;
      padding: 20px 20px 25px;
      text-align: center;
      box-shadow: 0 8px 25px rgba(0,0,0,0.2);
      transition: transform 0.3s ease;
      cursor: pointer;
    }

    .card:hover {
      transform: translateY(-5px);
    }

    .card h3 {
      margin: 10px 0 5px;
    }

    .card p {
      font-size: 14px;
      color: #666;
    }

    .card-img {
      width: 60px;
      height: 60px;
      object-fit: contain;
      margin-bottom: 10px;
    }

    .support-btn {
      position: fixed;
      bottom: 25px;
      right: 25px;
      background: #ff6b6b;
      color: white;
      border: none;
      border-radius: 50%;
      width: 60px;
      height: 60px;
      font-size: 26px;
      cursor: pointer;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
      z-index: 999;
      transition: background 0.3s ease;
    }

    .support-btn:hover {
      background: #ff4757;
    }

    .support-popup {
      position: fixed;
      bottom: 100px;
      right: 25px;
      background: white;
      color: #333;
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.2);
      padding: 20px;
      width: 300px;
      display: none;
      animation: slideUp 0.4s ease;
      z-index: 999;
    }

    .support-popup h4 {
      margin-top: 0;
      margin-bottom: 10px;
    }

    .support-popup p {
      font-size: 14px;
    }

    @keyframes slideUp {
      from { transform: translateY(20px); opacity: 0; }
      to   { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>

  <div class="overlay"></div>

  <div class="container">
    <h1>Xin chào, <%= user.username %> 👋</h1>

    <div class="balance-box">
      <p>💰 Xu: <strong><%= user.balance %></strong></p>
      <p>🪙 Robux: <strong><%= user.robux %></strong></p>
    </div>

    <div class="grid">
      <div class="card" onclick="location.href='/withdraw'">
        <img src="/images/withdraw.png" alt="Withdraw" class="card-img">
        <h3>Rút Robux</h3>
        <p>Chuyển Robux về tài khoản Roblox</p>
      </div>

      <div class="card" onclick="location.href='/spin'">
        <img src="/images/sp.jpg" alt="Spin" class="card-img">
        <h3>Quay Hộp Quà</h7>
        <p>Nhận Robux ngẫu nhiên cực hot</p>
      </div>

      <div class="card" onclick="location.href='/topup'">
        <img src="/images/napthe.jpg" alt="Topup" class="card-img">
        <h3>Nạp Tiền</h3>
        <p>Nạp xu qua thẻ cào hoặc ATM</p>
      </div>

      <div class="card" onclick="location.href='/shop'">
        <img src="/images/shop.png" alt="Shop" class="card-img">
        <h3>Shop Acc</h3>
        <p>Mua tài khoản Roblox cực VIP</p>
      </div>

      <div class="card" onclick="location.href='/history'">
        <img src="/images/history.png" alt="History" class="card-img">
        <h3>Lịch Sử</h3>
        <p>Xem lịch sử mua acc và giao dịch</p>
      </div>
    </div>
  </div>

  <!-- Nút hỗ trợ -->
  <button class="support-btn" onclick="toggleSupport()">💬</button>

  <!-- Popup hỗ trợ -->
  <div class="support-popup" id="supportPopup">
    <h4>Hỗ trợ</h4>
    <p>tiktok: <strong>@robux_napre </strong></p>
    <p>Email: <strong>support@robloxshop.vip</strong></p>
  </div>

  <script>
    function toggleSupport() {
      const popup = document.getElementById("supportPopup");
      popup.style.display = popup.style.display === "block" ? "none" : "block";
    }
  </script>

</body>
</html>
