<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Trang quản trị</title>
  <link rel="stylesheet" href="/public/style.css">
  <style>
    body {
      background: #1e1e2f;
      color: white;
      font-family: 'Segoe UI', sans-serif;
      padding: 30px;
    }
    h2 {
      text-align: center;
      color: #ffcc00;
      margin-bottom: 30px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      background: rgba(255,255,255,0.05);
    }
    th, td {
      padding: 12px;
      text-align: center;
      border-bottom: 1px solid #555;
    }
    input[type="number"] {
      width: 80px;
      padding: 4px;
    }
    form {
      display: flex;
      gap: 8px;
      justify-content: center;
    }
    .btn {
      padding: 5px 10px;
      border: none;
      background: #ffaa00;
      color: #000;
      border-radius: 5px;
      cursor: pointer;
    }
    .btn:hover {
      background: #ffcc00;
    }
  </style>
</head>
<body>
  <h2>👑 Quản trị viên: <%= user.username %></h2>

  <table>
    <thead>
      <tr>
        <th>Username</th>
        <th>Xu</th>
        <th>Robux</th>
        <th>Cộng Xu / Robux</th>
        <th>Xóa</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(u => { %>
        <tr>
          <td><%= u.username %></td>
          <td><%= u.balance %></td>
          <td><%= u.robux %></td>
          <td>
            <form action="/admin/update" method="POST">
              <input type="hidden" name="username" value="<%= u.username %>">
              <input type="number" name="xu" placeholder="Xu">
              <input type="number" name="robux" placeholder="Robux">
              <button class="btn" type="submit">✅ Cộng</button>
            </form>
          </td>
          <td>
            <form action="/admin/delete" method="POST" onsubmit="return confirm('Xác nhận xoá user <%= u.username %>?')">
              <input type="hidden" name="username" value="<%= u.username %>">
              <button class="btn" style="background: #ff5555;" type="submit">🗑️ Xoá</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
